---
- name: install and check python
  hosts: all
  tasks:
    - name: install python
      yum:
        name: python3
        state: latest
    - name: check
      shell:
        cmd: python3 --version
      register: output

    - name: print
      debug:
        msg: "{{ output.stdout }}"
