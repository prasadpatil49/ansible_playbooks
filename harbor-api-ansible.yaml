---
- name: harbor api testing
  hosts: localhost
  gather_facts: no
  tasks:
    - name: hit harbor api
      uri:
        url: https://harbor.dev.mlops.com/api/v2.0/users
        user: "prasad"
        password: "Prasad@123"
        method: GET
        force_basic_auth: yes
        validate_certs: no

      register: apiresult

    - name: print output
      debug: 
        var: apiresult
